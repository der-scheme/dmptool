<%# This partial makes maintaining translations for several kinds of submit
    buttons easier. Several helpers are available through the ApplicationHelper
    class. %>
<% content ||= t(tid || '.submit') %>
<% attributes ||= {} %>
<%= submit_tag content, **attributes %>
