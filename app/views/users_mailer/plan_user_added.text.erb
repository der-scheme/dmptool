<%= (@recipient == @user ? "You've been" : "#{@user.full_name} has been") %> added to the plan "<%= @plan.name %>" as a <%= @user.owner? ? 'owner' : 'co-owner' %>.

<%= render partial: 'information_link',
           locals: {title: 'DMP Overview', url: edit_plan_url(@plan)} %>
