Your user account has had the following role(s) assigned to it.

<%= @granted_roles %>

You currently have the following roles.

<%= @all_roles %>

<%= render partial: 'information_email',
           locals: {contact: 'the DMP Administrator', email: 'uc3@ucop.edu'} %>
