<%= (@user == @vars[:new_user] ? "You've been" : "#{@vars[:new_user].full_name} has been") %> added to the plan "<%= @plan.name %>" as a <%= @vars[:new_user_type] %>.

If you have questions pertaining to this action, please visit the DMP Overview page at <%= edit_plan_url(@plan) %>
