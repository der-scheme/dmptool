<% if @resource_context.new_record? %>
  	<%= link_to t('.choose_template_button'), requirements_templates_path,
								method: :get, class: 'btn pull-left', role: 'button' %>
<% else %>
	<% if user_role_in?(:dmp_admin) %>
  	<%= link_to t('.customizations_button'), resource_contexts_path,
								method: :get, class: 'btn pull-left', role: 'button' %>
  <%end%>
<% end %>
<% unless @resource_context.new_record? %>
  <%= link_to t('.details_button'), customization_requirement_path(@resource_context),
							class: 'btn btn-green pull-right', role: 'button' , id: 'next_btn' %>
<% end %>