$("#activate_link_<%= @requirements_template.id %>").text("<%= t('.toggle_status_link', template: @requirements_template) %>");
$("#status_<%= @requirements_template.id%>").text("<%= t_enum(@requirements_template, :active) %>");